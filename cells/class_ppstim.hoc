begintemplate ppstim

	public pp, connect_pre, is_art, acell
	create acell
	objref pp

	proc init() {
			
	}

	func is_art() {return 1}
	proc connect_pre() {acell $o2 = new IClamp(.5)
            $o2.del = 0
            $o2.dur = 10
            $o2.amp = 1}

endtemplate ppstim
